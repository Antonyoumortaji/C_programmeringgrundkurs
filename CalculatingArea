#include <stdio.h>
#include <math.h> 
int main() {
const double PI = 3.1415;
double radius , area;
printf("Enter the radious:\n"); 
scanf("%lf", &radius);
area = PI * pow(radius, 2); // or PI * radius * radius circumference = PI * radius * 2; 
printf("Area: %lf", area);
return 0;
}
